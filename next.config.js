/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/appointment',
  assetPrefix: '/appointment',
};

module.exports = nextConfig;